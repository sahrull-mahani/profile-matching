<?php

namespace App\Models;

use CodeIgniter\Model;

class AspekM extends Model
{
    protected $table = 'aspek';
    protected $allowedFields = array('aspek_penilaian', 'persentase', 'core', 'secondary');
    protected $returnType     = 'object';
    protected $useSoftDeletes = false;

    protected $useTimestamps = true;
    protected $createdField  = 'created_at';
    protected $updatedField  = 'updated_at';
    protected $deletedField  = 'deleted_at';

    protected $validationRules = [
        'aspek_penilaian' => 'required|max_length[150]',
        'persentase' => 'required|max_length[3]',
        'core' => 'required|max_length[5]',
        'secondary' => 'required|max_length[5]',
    ];

    protected $validationMessages = [
        'aspek_penilaian' => ['required' => 'tidak boleh kosong', 'max_length' => 'Maximal 150 Karakter'],
        'persentase' => ['required' => 'tidak boleh kosong', 'max_length' => 'Maximal 3 Karakter'],
        'core' => ['required' => 'tidak boleh kosong', 'max_length' => 'Maximal 5 Karakter'],
        'secondary' => ['required' => 'tidak boleh kosong', 'max_length' => 'Maximal 5 Karakter'],
    ];
    private function _get_datatables()
    {
        $column_search = array('aspek_penilaian', 'persentase', 'core', 'secondary');
        $i = 0;
        foreach ($column_search as $item) { // loop column 
            if ($_GET['search']) {
                if ($i === 0) {
                    $this->groupStart();
                    $this->like($item, $_GET['search']);
                } else {
                    $this->orLike($item, $_GET['search']);
                }
                if (count($column_search) - 1 == $i)
                    $this->groupEnd();
            }
            $i++;
        }
        if (isset($_GET['order'])) {
            $this->orderBy($_GET['sort'], $_GET['order']);
        } else {
            $this->orderBy('id', 'asc');
        }
    }
    public function get_datatables()
    {
        $this->_get_datatables();
        $limit = isset($_GET['limit']) ? $_GET['limit'] : 0;
        $offset = isset($_GET['offset']) ? $_GET['offset'] : 0;
        return $this->findAll($limit, $offset);
    }
    public function total()
    {
        $this->_get_datatables();
        if ($this->tempUseSoftDeletes) {
            $this->where($this->table . '.' . $this->deletedField, null);
        }
        return $this->get()->getNumRows();
    }
}
/* End of file AspekM.php */
/* Location: ./app/models/AspekM.php */
/* Please DO NOT modify this information : */
/* Generated by Harviacode Codeigniter CRUD Generator 2022-07-31 08:03:25 */